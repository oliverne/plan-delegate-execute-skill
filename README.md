# Plan, Delegate & Execute

Modern coding models often need fewer workflow instructions, not more. This skill is an experiment in reducing spec-driven, delegated implementation to a handful of durable anchors and letting the model infer the rest.
